# Add products to wishlist
Function AddToWishlist:
    If request body is invalid:
        Return HTTP 400 Bad Request with message "Invalid request"

    result = saveWishlist(userID, productID)
    If result is error:
        Return HTTP 500 Internal Server Error with message "Failed to add to wishlist"

    Return HTTP 201 Created with message "Added to wishlist"






# Get current user’s wishlist
Function GetWishlist:
    If userID is not found:
        Return HTTP 401 Unauthorized with message "User not authenticated"

    wishlistItems = fetchWishlistByUserID(userID)
    If error while fetching:
        Return HTTP 500 Internal Server Error with message "Failed to retrieve wishlist"

    Return HTTP 200 OK with data: wishlistItems





# Remove product from wishlist
Function RemoveFromWishlist(productID):
    If userID is not found:
        Return HTTP 401 Unauthorized with message "User not authenticated"

    result = deleteWishlistItem(userID, productID)
    If result is error:
        Return HTTP 500 Internal Server Error with message "Failed to remove item from wishlist"

    Return HTTP 200 OK with message "Item removed from wishlist"






# Delete all items from wishlist
Function ClearWishlist:
    If userID is not found:
        Return HTTP 401 Unauthorized with message "User not authenticated"

    result = clearWishlistByUserID(userID)
    If result is error:
        Return HTTP 500 Internal Server Error with message "Failed to clear wishlist"

    Return HTTP 200 OK with message "Wishlist cleared"
